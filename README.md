# farol

Read a branch in the order the person who wrote it would walk you through.

Every automated reviewer today works post-hoc: it receives a diff and tries to
reconstruct intent by reverse engineering. The session that implemented the
change knows what the diff does not carry — the approach that was tried and
reverted, the constraint discovered halfway, which file is the heart of the
change and which only exists so the rest compiles. Today that is thrown away
when the session closes.

farol keeps it. The session writes a **map** through the CLI; farol serves the
diff in that order, grouped into blocks, with the explanations alongside.

It is not a diff viewer. Without a map there is nothing to show, and it says so
rather than degrading into a worse version of tools that already do that well.

## Using it

```bash
farol scope          # the files under review, as farol sees them
farol map derive     # start (or continue) the map for this commit
farol serve          # read it in a browser
```

The map itself is normally written by the `review-map` skill at the end of the
session that implemented the change. By hand it looks like this:

```bash
farol block add recover-link \
  --title "Recover the link before signing" \
  --context "The first version created generic abstractions and was reverted." \
  services/acceptance.go

farol file add recover-link services/db.go --note "The deletions here are not an additional change."
farol line add recover-link services/acceptance.go 82-116 --note "This ordering is deliberate."
farol skim add go.sum --reason "regenerated by the dependency bump"
farol map check      # non-zero while anything is unassigned or undecided
```

`farol --help` and `farol <command> --help` list the rest.

### Reading

`j`/`k` move between files in reading order, `n` jumps to the next unread one,
`e` marks the current file read, `[`/`]` move between blocks, `?` shows the
keys.

## How it behaves

**Base and head.** `main`, falling back to `master`; head is the branch you are
standing on. The diff is taken from the merge base, so what the base branch did
after you branched stays out of your review — `--direct` opts out. Detached
HEAD is refused: there is no branch name to key state on.

**State lives with the worktree.** Everything is stored under the worktree's own
git dir, so removing a worktree takes the review with it. That is the intended
cleanup, not a limitation. It also means the map does not travel: farol is a
tool for reading your own change before you open the pull request.

**Progress survives new commits.** A file you marked read stays read until its
contents change; a rebase that only shifts context lines does not reopen it,
because the hash is of the file, not of the diff.

**Maps are derived, not rebuilt.** Each commit gets a version inheriting the one
before. Whoever is reading may be halfway through, and rebuilding from scratch
would reshuffle the block boundaries under them.

**Line notes are the fragile part.** When a file changes far from a note, the
range is recomputed arithmetically. When the change reaches into the code the
note covered, the note is *deactivated* rather than moved or deleted: the prose
is kept along with the code it used to cover, and `farol map derive` asks you to
restore or discard it. Guessing would put a confident note on unrelated code,
which is worse than showing nothing.

## Building

```bash
mise use -g rust@latest just@latest
just build     # vite build, then cargo build --release
just test
just check     # layers, tests, clippy, fmt
```

The frontend is embedded in the binary, so a release is one file. `just layers`
enforces the one architectural rule worth machine-checking: HTTP lives in
`src/server/` and nothing else knows it exists.

## Layout

```
src/
├── cmd/         CLI entry points
├── server/      HTTP, SSE, embedded assets
├── shared/      errors, git dir and store paths
├── diff/        derived from git, never persisted
├── map/         blocks, order, notes — written by the skill
└── progress/    what you have read — written by the server
web/             vite + react
```

Each of the three parts is split into `domain`, `application`, `infra` and
`presentation`. The parts were chosen by who writes them: the skill writes
`map`, the server writes `progress`, nobody writes `diff`. That is the same line
that separates the files on disk, which is why two processes never contend.
